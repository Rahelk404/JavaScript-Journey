const mobile = {
    Brand: 'samsung',
    price: 30000,
    color: 'silver',
    camera: '12mp',
    isNew: true
}

const final = Object.keys(mobile);
console.log(final);

for (const final of keys){
    console.log(keys);
}